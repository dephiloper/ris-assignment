#include "loginmessagehandler.h"

void LoginMessageHandler::handle(const NetMessage& message) {
    // TODO add player to currently active players
    std::cout << "handled by loginmessagehandler" << std::endl;
}
